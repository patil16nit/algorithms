#include<iostream>
#include<list>

using namespace std;

class Graph {
    int v;
    list<int> adj;
    public:
        Graph(int v);
        void addEdge(int s, int w);
        
};

int main {

    return 0;
}
